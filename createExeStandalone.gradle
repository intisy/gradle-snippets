project.afterEvaluate {
    tasks.create('createExeStandalone', edu.sc.seis.launch4j.tasks.Launch4jLibraryTask) {
        mustRunAfter("createExe")
        outfile = "${artifact_name}-standalone.exe"
        jarTask = project.tasks.shadowJar
    }
}