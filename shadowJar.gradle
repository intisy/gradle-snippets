apply plugin: "com.github.johnrengelman.shadow" version "8.1.1"

shadowJar {
    archiveFileName = "${artifact_name}-standalone.jar"
    manifest {
        attributes "Main-Class": "${group}.Main"
    }
}